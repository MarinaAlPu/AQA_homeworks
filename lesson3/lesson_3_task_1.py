# создайте новый экземпляр `User` и сохраните его в переменную `my_user`
# вызовите все методы у объекта в переменной `my_user`

from user import User

my_user = User("Marina", "Pudovkina")

my_user.sayName()
my_user.sayFamily()
my_user.sayNameFamily()